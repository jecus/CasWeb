@using BusinessLayer.Views

@{
    ViewData["Title"] = "Quotation";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var quotation = (List<RequestForQuotationView>)ViewData["RequestForQuotations"];
}

<div class="card">
    <div class="card-header">
        <h5>Request For Quotation</h5>
    </div>
    <div class="card-block">
        <div class="dt-responsive table-responsive">
            <table id="footer-search" class="table table-striped table-bordered nowrap">
                <thead>
                    <tr>
                        <th> </th>
                        <th>Order No</th>
                        <th>Title</th>
                        <th>Opening Date</th>
                        <th>Publishing Date</th>
                        <th>Closing Date</th>
                        <th>Author</th>
                        <th>Published By</th>
                        <th>Closed By</th>
                        <th>Remark</th>
                        @*<th>Signer</th>*@
                    </tr>
                </thead>
                <tbody>

                    <div class="modal fade" id="large-Modal" tabindex="-1" role="dialog" style="display: none; z-index: 1050;" aria-hidden="true">
                        <div class="modal-dialog modal-lg" role="document">
                            <div id="dialogContent" class="modal-content">

                            </div>
                        </div>
                    </div>

                    @foreach (var quot in quotation)
                    {
                        <tr>
                            <td>

                                @*<a href=@Url.Action("Modal", "Document", new {id = spec.Id}) , class="modalLargeBtn btn waves-effect waves-dark btn-primary btn-outline-primary btn-icon">
                                        <i class="fa fa-pencil"></i>
                                    </a>*@

                                @*@Html.ActionLink("", "Modal", "Document", new { id = spec.Id}, new { @class = "modalLargeBtn btn waves-effect waves-dark btn-danger btn-outline-danger btn-icon" })*@
                            </td>
                            <td>@quot.Number</td>
                            <td>@quot.Title</td>
                            <td>@quot.OpeningDate</td>
                            <td>@quot.PublishingDate</td>
                            <td>@quot.ClosingDate</td>
                            <td>@quot.Author</td>
                            <td>@quot.PublishedByUser</td>
                            <td>@quot.CloseByUser</td>
                            <td>@quot.Remarks</td>
                            @*<td>@init.</td>*@
                        </tr>
                    }

                </tbody>
                <tfoot>
                    <tr>
                        <th> </th>
                        <th>Order No</th>
                        <th>Title</th>
                        <th>Opening Date</th>
                        <th>Publishing Date</th>
                        <th>Closing Date</th>
                        <th>Author</th>
                        <th>Published By</th>
                        <th>Closed By</th>
                        <th>Remark</th>
                        @*<th>Signer</th>*@
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>

